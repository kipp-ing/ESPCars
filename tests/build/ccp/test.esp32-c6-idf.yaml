# ESP32-C6 CCP config wrapper.
esphome:
  name: ccp-c6
esp32:
  board: esp32-c6-devkitc-1
  framework:
    type: esp-idf
logger:
external_components:
  - source:
      type: local
      path: ../../../components
packages:
  ccp: !include common.yaml
