esphome:
  name: uds-partition-c6

esp32:
  board: esp32-c6-devkitc-1
  framework:
    type: esp-idf

logger:

external_components:
  - source:
      type: local
      path: ../../../components

packages:
  uds: !include common_partition.yaml
